doctype html  
html( lang='en' ) 
	head
		block title
		meta( charset='utf-8' )
		meta( http-equiv='X-UA-Compatible', content='IE=edge' )
		meta( name='viewport', content='width=device-width, initial-scale=1.0' )
		meta( name='description', content='API for TApp translations via NFC tags' )
		
		//- Normalize css
		link( href="https://cdnjs.cloudflare.com/ajax/libs/normalize/7.0.0/normalize.min.css", rel="stylesheet", type='text/css')
		//- Custom Styles
		link( href='/stylesheets/style.css', rel='stylesheet', type='text/css' )

	body( ng-app='tapp' )
		header
			include shared/header
		main
			block content

		footer
			include shared/footer

		//- jQuery
		script( src="https://code.jquery.com/jquery-3.2.1.min.js" integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4=" crossorigin="anonymous" )
		//- Angular JavaScript
		script( src='libs/angular/angular.js', rel='text/javascript' )

		script.
			$('document').ready(function() {
				var curLng = window.location.href.slice(-2) || 'en';
				var select = $('#lng option:eq(0)').text(curLng.toUpperCase());;
			});

		
		block js

		